export default function StartSection({ onStart }) {
  return (
    <div>
      <h1>Every Prompt Drinks Water</h1>
      <p>The invisible cost of artificial intelligence.</p>
      <button onClick={onStart}>Discover the Truth</button>
    </div>
  );
}
